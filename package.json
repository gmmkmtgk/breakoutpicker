{
  "name": "breakoutpicker",
  "private": true,
  "scripts": {
    "start": "npm --prefix web start",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "lint": "npm --prefix web run lint",
    "lint:fix": "npm --prefix web run lint:fix",
    "prettier": "npm --prefix web run prettier"
  }
}
